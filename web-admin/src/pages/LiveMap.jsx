import { Box, Typography } from '@mui/material';

export default function LiveMap() {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: 'bold' }}>
        Live Fleet Map
      </Typography>
      <Typography>Real-time truck tracking map interface...</Typography>
    </Box>
  );
}
