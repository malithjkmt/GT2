import { Box, Typography } from '@mui/material';

export default function DriverManagement() {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: 'bold' }}>
        Driver Management
      </Typography>
      <Typography>Driver management interface...</Typography>
    </Box>
  );
}
