import { Box, Typography } from '@mui/material';

export default function EditRoute() {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: 'bold' }}>
        Edit Route
      </Typography>
      <Typography>Route editing interface...</Typography>
    </Box>
  );
}
