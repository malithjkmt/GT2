import { Box, Typography } from '@mui/material';

export default function TruckManagement() {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: 'bold' }}>
        Truck Management
      </Typography>
      <Typography>Truck fleet management interface...</Typography>
    </Box>
  );
}
