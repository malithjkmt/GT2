import { Box, Typography } from '@mui/material';

export default function FeedbackManagement() {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: 'bold' }}>
        Feedback Management
      </Typography>
      <Typography>Feedback management interface...</Typography>
    </Box>
  );
}
