import { Box, Typography } from '@mui/material';

export default function AddRoute() {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: 'bold' }}>
        Add New Route
      </Typography>
      <Typography>Route creation form with map drawing tools...</Typography>
    </Box>
  );
}
